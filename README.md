
## 📄 PassGen

### 🔐 Overview
`PassGen` is a React component built with Next.js and Tailwind CSS. It generates secure random passwords with customizable length and includes copy-to-clipboard functionality for user convenience.

---

### 🚀 Features
- Generate random passwords with uppercase, lowercase, numbers, and symbols.
- Customizable password length (4-30 characters) with slider and input controls.
- Toggle character types: Uppercase, Lowercase, Numbers, and Symbols.
- Password strength indicator (Weak, Average, Strong).
- Copy password to clipboard with a single click.
- Visual feedback when password is copied.
- Responsive design that works on mobile and desktop devices.

---

### 🧱 Technologies Used
- **React** with Hooks (`useState`, `useRef`)
- **Next.js** (App Router structure)
- **Tailwind CSS** for styling
- **Next/Image** for optimized image rendering

---

---

### 📂 File Structure
```
src/app/
├── components/
│   ├── Container.jsx      → Main container component
│   └── PasswordBox.jsx    → Password generator component
├── layout.js
├── page.js
└── globals.css

public/
├── copy_icon.png         → Copy button icon
└── generate_icon.png     → Generate button icon
```

---

### 📝 How It Works
1. On initial load, a password can be generated by clicking the “Generate Password” button.
2. The password includes at least one character from each category: uppercase, lowercase, number, and symbol.
3. The length can be set using the input box inside the toggleable “More Options” menu.
4. The password can be copied with the copy icon, and a temporary message appears confirming it.

---

